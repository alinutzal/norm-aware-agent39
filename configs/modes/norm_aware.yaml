# Norm-aware mode: NORA with agent orchestration
modes: norm_aware

agent:
  enabled: true
  norm_aware: true
  reactive: true
  
  observe_frequency: every_epoch
  decide_frequency: every_epoch
  action_threshold: 0.1

logging:
  agent_logs: true
  decision_logs: true
  norm_logs: true
