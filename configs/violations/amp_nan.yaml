# AMP NaN/Inf violation
name: amp_nan
description: "Trigger AMP instability by disabling GradScaler and warmup (often yields NaN/Inf)."
severity: high

amp:
  enabled: true
  grad_scaler: false
  loss_nan_policy: ignore

scheduler:
  warmup_epochs: 0

optimizer:
  lr: 8e-4
