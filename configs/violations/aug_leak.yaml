# Augmentation leakage violation
name: aug_leak
description: "Apply stochastic augmentation to evaluation data (protocol violation)."
severity: high

dataset:
  eval_augment:
    random_crop: true
    crop_padding: 4
    hflip: true
