# Incomplete checkpoint violation
name: checkpoint_incomplete
description: "Save incomplete checkpoints (missing optimizer/scheduler/scaler)."
severity: medium

checkpoint:
  include_optimizer: false
  include_scheduler: false
  include_scaler: false
